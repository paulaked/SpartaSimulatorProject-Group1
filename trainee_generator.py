from random import randrange


def rand_trainee():  # calling function returns a new random number. Need to assign to variable each month so maths
    # and print statements match
    return randrange(20, 31, 1)  # generates number between 20-30, increment 1.
